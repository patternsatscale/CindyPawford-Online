// Cindy Pawford — era zero.
// The page is intentionally quiet: no behavior, no motion.
// (The joke lives in the HTML source, not here. Don't look — find it yourself.)
(function () {
  "use strict";
  // Intentionally nothing to do on era zero.
})();
